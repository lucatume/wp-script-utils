// Generated by CoffeeScript 1.9.2
(function() {
  (function($) {
    var hookBus, runHook;
    hookBus = window.hookBus = window.hookBus || _.extend({}, Backbone.Events);
    runHook = function(hookName, p) {
      var error, hook, message;
      hook = hookName + ':' + p;
      try {
        return hookBus.trigger(hook);
      } catch (_error) {
        error = _error;
        message = 'Exception in ' + hook + ' - ' + error.message;
        if (console) {
          return console.log(message);
        }
      }
    };
    return $(document).ready(function() {
      var i, j, k, len, len1, len2, p, priorities, results;
      priorities = _.range(11);
      for (i = 0, len = priorities.length; i < len; i++) {
        p = priorities[i];
        runHook('initBase', p);
      }
      for (j = 0, len1 = priorities.length; j < len1; j++) {
        p = priorities[j];
        runHook('initFunctional', p);
      }
      results = [];
      for (k = 0, len2 = priorities.length; k < len2; k++) {
        p = priorities[k];
        results.push(runHook('initUi', p));
      }
      return results;
    });
  })(jQuery);

}).call(this);
